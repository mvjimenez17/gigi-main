// DOM - DOCUMENT OBJECT MODEL

let val;

val = document;
val = document.all;
val = document.body;
val = document.doctype;
val = document.domain;
val = document.URL;

val = document.forms;
val = document.forms[0];

val = document.links;
val = document.links[0];
val = document.links[0].className;
val = document.links[0].getAttribute("href");
val = document.links[0].getAttribute("title");

val = document.images;
val = document.images[0];
val = document.images[0].getAttribute("src");

console.log(val);

