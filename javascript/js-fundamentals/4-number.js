const num1 = 100;
const num2 = 50;


let val;

// Simple Msth with Numbers
val = num1 + num2; // Add
val = num1 - num2; // Sub
val = num1 * num2; // Multiply
val = num1 / num2; // Division
val = num1 % num2; // Reminder


// Math Object
val = Math.PI;
val = Math.round(2.9);
val = Math.ceil(2.1); // Upper number
val = Math.floor(2.9); // Lower number
val = Math.sqrt(64);
val = Math.abs(-5);
val = Math.min(-5,1,2,5,6,7);
val = Math.max(-5,1,2,5,6,7);
val = Math.random();

val = Math.floor(Math.random() * 20)


console.log(val);